using UnityEngine;

public class FlarePositioning : MonoBehaviour
{
    void Update()
    {
        //transform.position = new Vector3(Camera.main.GetComponent<CameraOrbit>().CurrentDistance * 4, 6.5f, -3f);
    }
}
